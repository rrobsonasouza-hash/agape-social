rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regras multi-tenant serão implementadas na Fase 1.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
