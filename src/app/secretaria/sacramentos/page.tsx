"use client";
import { FormEvent,useCallback,useEffect,useMemo,useState } from "react";
import { BookOpen,FileText,History,Pencil,Plus,Search } from "lucide-react";
import toast from "react-hot-toast";
import { ProtectedArea } from "@/components/auth/ProtectedArea";
import { ModuleHeader } from "@/components/layout/ModuleHeader";
import { obterTokenAcesso } from "@/lib/auth/client-session";
type Registro={id:string;sacramento:string;pessoaNome:string;pessoaNascimento:string|null;pessoaNaturalidade:string;paiNome:string;maeNome:string;padrinhos:string[];dataSacramento:string;celebrante:string;localSacramento:string;livro:string;folha:string;termo:string;numeroRegistro:string;observacoes:string;ativo:boolean;criadoPorNome:string;createdAt:string};type Historico={id:string;acao:string;descricao:string;criadoPorNome:string;createdAt:string};
const nomes:Record<string,string>={BATISMO:"Batismo",PRIMEIRA_COMUNHAO:"Primeira Comunhão",CRISMA:"Crisma",MATRIMONIO:"Matrimônio"};const vazio=()=>({id:"",sacramento:"BATISMO",pessoaNome:"",pessoaNascimento:"",pessoaNaturalidade:"",paiNome:"",maeNome:"",padrinho1:"",padrinho2:"",dataSacramento:"",celebrante:"",localSacramento:"",livro:"",folha:"",termo:"",numeroRegistro:"",observacoes:"",ativo:true});
async function api<T>(url="",init?:RequestInit){const token=await obterTokenAcesso();const r=await fetch(`/api/secretaria/sacramentos${url}`,{...init,headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`}});const d=await r.json();if(!r.ok)throw new Error(d.erro||"Operação não concluída.");return d as T;}
function Conteudo(){const[registros,setRegistros]=useState<Registro[]>([]);const[busca,setBusca]=useState("");const[filtro,setFiltro]=useState("TODOS");const[form,setForm]=useState(vazio);const[aberto,setAberto]=useState(false);const[detalhe,setDetalhe]=useState<{registro:Registro;historico:Historico[]}|null>(null);const[salvando,setSalvando]=useState(false);const carregar=useCallback(async()=>{try{setRegistros(await api<Registro[]>());}catch(e){toast.error(e instanceof Error?e.message:"Não foi possível carregar os registros.");}},[]);useEffect(()=>{void carregar();},[carregar]);
  const visiveis=useMemo(()=>registros.filter(r=>(filtro==="TODOS"||r.sacramento===filtro)&&(!busca||`${r.pessoaNome} ${r.livro} ${r.folha} ${r.termo} ${r.numeroRegistro} ${r.maeNome} ${r.paiNome}`.toLowerCase().includes(busca.toLowerCase()))),[registros,busca,filtro]);
  function editar(r:Registro){setForm({id:r.id,sacramento:r.sacramento,pessoaNome:r.pessoaNome,pessoaNascimento:r.pessoaNascimento||"",pessoaNaturalidade:r.pessoaNaturalidade,paiNome:r.paiNome,maeNome:r.maeNome,padrinho1:r.padrinhos[0]||"",padrinho2:r.padrinhos[1]||"",dataSacramento:r.dataSacramento,celebrante:r.celebrante,localSacramento:r.localSacramento,livro:r.livro,folha:r.folha,termo:r.termo,numeroRegistro:r.numeroRegistro,observacoes:r.observacoes,ativo:r.ativo});setAberto(true)}
  async function abrirDetalhe(id:string){try{setDetalhe(await api(`?id=${id}`));}catch(e){toast.error(e instanceof Error?e.message:"Não foi possível abrir o histórico.");}}
  async function salvar(e:FormEvent){e.preventDefault();setSalvando(true);try{const payload={...form,pessoaNascimento:form.pessoaNascimento||null,padrinhos:[form.padrinho1,form.padrinho2].filter(Boolean)};await api("",{method:form.id?"PATCH":"POST",body:JSON.stringify(payload)});toast.success("Registro sacramental salvo.");setAberto(false);setForm(vazio());await carregar();}catch(e){toast.error(e instanceof Error?e.message:"Não foi possível salvar.");}finally{setSalvando(false)}}
  return <main className="min-h-screen bg-slate-100"><ModuleHeader titulo="Registros sacramentais"/><div className="mx-auto max-w-7xl space-y-4 p-4 sm:p-6"><section className="flex flex-col gap-3 rounded-2xl border bg-white p-4 shadow-sm sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-slate-400" size={20}/><input value={busca} onChange={e=>setBusca(e.target.value)} placeholder="Buscar nome, pais, livro, termo ou número" className="min-h-12 w-full rounded-xl border pl-10 pr-3"/></div><select value={filtro} onChange={e=>setFiltro(e.target.value)} className="min-h-12 rounded-xl border bg-white px-3"><option value="TODOS">Todos os sacramentos</option>{Object.entries(nomes).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><button onClick={()=>{setForm(vazio());setAberto(true)}} className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 font-black text-white"><Plus size={18}/>Novo registro</button></section><section className="grid gap-3 lg:grid-cols-2">{visiveis.map(r=><article key={r.id} className={`rounded-2xl border bg-white p-5 shadow-sm ${!r.ativo?"opacity-55":""}`}><div className="flex items-start justify-between gap-3"><div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-blue-700">{nomes[r.sacramento]}</span><h2 className="mt-3 text-lg font-black">{r.pessoaNome}</h2><p className="text-sm text-slate-500">{new Date(`${r.dataSacramento}T12:00:00`).toLocaleDateString("pt-BR")} • Livro {r.livro}, folha {r.folha}, termo {r.termo}</p><p className="text-sm text-slate-500">Registro nº {r.numeroRegistro}</p></div><BookOpen className="text-blue-200" size={32}/></div><div className="mt-4 grid grid-cols-3 gap-2"><button onClick={()=>editar(r)} className="inline-flex min-h-11 items-center justify-center gap-1 rounded-xl border font-bold text-blue-700"><Pencil size={17}/>Editar</button><button onClick={()=>void abrirDetalhe(r.id)} className="inline-flex min-h-11 items-center justify-center gap-1 rounded-xl border font-bold"><History size={17}/>Histórico</button><a href={`/secretaria/sacramentos/${r.id}/certidao`} className="inline-flex min-h-11 items-center justify-center gap-1 rounded-xl bg-slate-900 px-2 font-bold text-white"><FileText size={17}/>Certidão</a></div></article>)}{!visiveis.length&&<div className="rounded-2xl bg-white p-10 text-center text-slate-500 lg:col-span-2">Nenhum registro encontrado.</div>}</section></div>
    {aberto&&<div className="fixed inset-0 z-50 grid place-items-end bg-slate-950/50 sm:place-items-center"><form onSubmit={salvar} className="max-h-[96vh] w-full overflow-y-auto rounded-t-3xl bg-white p-5 sm:max-w-3xl sm:rounded-3xl"><h2 className="text-xl font-black">{form.id?"Editar registro":"Novo registro sacramental"}</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-sm font-bold">Sacramento<select value={form.sacramento} onChange={e=>setForm({...form,sacramento:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border bg-white px-3">{Object.entries(nomes).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label className="text-sm font-bold">Data do sacramento<input type="date" value={form.dataSacramento} onChange={e=>setForm({...form,dataSacramento:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3" required/></label><label className="text-sm font-bold sm:col-span-2">Nome da pessoa<input value={form.pessoaNome} onChange={e=>setForm({...form,pessoaNome:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3 font-normal" required/></label><label className="text-sm font-bold">Nascimento<input type="date" value={form.pessoaNascimento} onChange={e=>setForm({...form,pessoaNascimento:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3"/></label><label className="text-sm font-bold">Naturalidade<input value={form.pessoaNaturalidade} onChange={e=>setForm({...form,pessoaNaturalidade:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3 font-normal"/></label><label className="text-sm font-bold">Nome do pai<input value={form.paiNome} onChange={e=>setForm({...form,paiNome:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3 font-normal"/></label><label className="text-sm font-bold">Nome da mãe<input value={form.maeNome} onChange={e=>setForm({...form,maeNome:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3 font-normal"/></label><label className="text-sm font-bold">Padrinho/testemunha 1<input value={form.padrinho1} onChange={e=>setForm({...form,padrinho1:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3 font-normal"/></label><label className="text-sm font-bold">Padrinho/testemunha 2<input value={form.padrinho2} onChange={e=>setForm({...form,padrinho2:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3 font-normal"/></label><label className="text-sm font-bold">Celebrante<input value={form.celebrante} onChange={e=>setForm({...form,celebrante:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3 font-normal"/></label><label className="text-sm font-bold">Local<input value={form.localSacramento} onChange={e=>setForm({...form,localSacramento:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-3 font-normal"/></label><div className="grid grid-cols-4 gap-2 sm:col-span-2"><label className="text-sm font-bold">Livro<input value={form.livro} onChange={e=>setForm({...form,livro:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-2" required/></label><label className="text-sm font-bold">Folha<input value={form.folha} onChange={e=>setForm({...form,folha:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-2" required/></label><label className="text-sm font-bold">Termo<input value={form.termo} onChange={e=>setForm({...form,termo:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-2" required/></label><label className="text-sm font-bold">Número<input value={form.numeroRegistro} onChange={e=>setForm({...form,numeroRegistro:e.target.value})} className="mt-1 min-h-12 w-full rounded-xl border px-2" required/></label></div><label className="text-sm font-bold sm:col-span-2">Observações<textarea value={form.observacoes} onChange={e=>setForm({...form,observacoes:e.target.value})} className="mt-1 min-h-24 w-full rounded-xl border p-3 font-normal"/></label>{form.id&&<label className="flex items-center gap-2 font-bold sm:col-span-2"><input type="checkbox" checked={form.ativo} onChange={e=>setForm({...form,ativo:e.target.checked})}/>Registro ativo</label>}</div><div className="mt-5 flex gap-2"><button disabled={salvando} className="min-h-12 flex-1 rounded-xl bg-blue-600 font-black text-white">{salvando?"Salvando...":"Salvar registro"}</button><button type="button" onClick={()=>setAberto(false)} className="rounded-xl border px-5 font-bold">Cancelar</button></div></form></div>}
    {detalhe&&<div className="fixed inset-0 z-50 grid place-items-end bg-slate-950/50 sm:place-items-center"><section className="max-h-[90vh] w-full overflow-y-auto rounded-t-3xl bg-white p-5 sm:max-w-xl sm:rounded-3xl"><h2 className="text-xl font-black">Histórico de {detalhe.registro.pessoaNome}</h2><div className="mt-4 space-y-4 border-l-2 border-slate-200 pl-4">{detalhe.historico.map(h=><div key={h.id}><p className="font-bold">{h.descricao}</p><p className="text-xs text-slate-500">{h.criadoPorNome} • {new Date(h.createdAt).toLocaleString("pt-BR")}</p></div>)}</div><button onClick={()=>setDetalhe(null)} className="mt-5 min-h-12 w-full rounded-xl border font-bold">Fechar</button></section></div>}
  </main>}
export default function SacramentosPage(){return <ProtectedArea><Conteudo/></ProtectedArea>}
